# Employee-Management-System
A simple Java-based Employee Management System that performs CRUD (Create, Read, Update, Delete) operations using a MySQL database. The application follows a DAO (Data Access Object) design pattern to manage employee records efficiently, demonstrating database connectivity, modular code structure, and basic console-based interaction.
